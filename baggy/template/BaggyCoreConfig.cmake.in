# this file is autogenerated by cmake

cmake_minimum_required(VERSION 3.14)

include(${CMAKE_CURRENT_LIST_DIR}/common.cmake)

get_filename_component(baggy_core_dir "${CMAKE_CURRENT_LIST_DIR}/.." ABSOLUTE)
set(baggy_core_include ${baggy_core_dir}/include)

# compile
set(CMAKE_CXX_STANDARD 14)

if (NOT TARGET baggy_core_lib)
    add_library(baggy_core_lib SHARED IMPORTED)
    target_include_directories(baggy_core_lib
            INTERFACE
            ${baggy_core_include}
            )
    set_target_properties(baggy_core_lib PROPERTIES
            IMPORTED_LOCATION ${baggy_core_dir}/lib/BaggyCore/libBaggyCore.so
            )
endif ()

# export config
set(BaggyCore_FOUND TRUE)
set(BaggyCore_INCLUDE_DIRS ${baggy_core_include})
set(BaggyCore_LIBRARIES baggy_core_lib)

# version
set(BaggyCore_VERSION @PROJECT_VERSION@)
set(BaggyCore_VERSION_MAJOR @PROJECT_VERSION_MAJOR@)
set(BaggyCore_VERSION_MINOR @PROJECT_VERSION_MINOR@)
set(BaggyCore_VERSION_PATCH @PROJECT_VERSION_PATCH@)

message(STATUS "BaggyCore found: ${BaggyCore_VERSION}")

# clear
unset(baggy_core_dir)
unset(baggy_core_include)


